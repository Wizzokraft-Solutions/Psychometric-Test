# ===== Sensitive source data — NEVER upload to GitHub =====
# Question banks, answer keys, scoring and the report template stay local only.
*.xlsx
*.docx
*.csv
~$*
# Parsed content (contains answer keys) — load into Supabase, never commit
content/

# ===== Node / build artifacts (added with the app later) =====
node_modules/
dist/
.env
.env*.local

# ===== Playwright e2e artifacts =====
test-results/
playwright-report/
blob-report/
playwright/.cache/

# ===== OS / editor cruft =====
Thumbs.db
.DS_Store

# GitHub Actions workflow � re-enable after 'gh auth refresh -s workflow'
.github/
